extends layout

block content
    form(action="contact/send", method="post")
        h1 #{title}
        .form-group
            label(for="name") Name
            input.form-control(type="text",id="name",name="name",placeholder="Enter Name")
        .form-group
            label(for="email") Email
            input.form-control(type="email",id="email",name="email",placeholder="Enter Email")
        .form-group
            label(for="message") Message
            textarea.form-control(id="message",name="message",placeholder="Enter Message")
        button.btn.btn-danger(type="submit") Submit
